package learnSearch;
import java.util.*;

/**
 * 实现B树，成本模型为页的访问次数。
 * 为2-3树的扩展，构建由键的副本组成的树。
 * 选择参数M(一般为偶数),每个节点不多余M-1个链接，不少于M/2个链接(保证查找路径不长）,根节点例外，但也
 * 不能少于两个。称为M阶B树
 * 查找或插入操作需要lgN/lgM到lgN/lg(M/2)间，实际情况下基本为常数。
 * @author guo
 *
 */
public class BTree {

}
